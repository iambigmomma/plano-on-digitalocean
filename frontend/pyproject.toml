[project]
name = "storybook-frontend"
version = "0.1.0"
description = "AI Storybook Generator — Web Frontend"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110",
    "uvicorn>=0.29",
    "httpx>=0.27",
]
