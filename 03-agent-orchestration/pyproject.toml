[project]
name = "storybook-demo"
version = "0.1.0"
description = "AI Storybook Generator — Plano + DigitalOcean Demo"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.0",
]

[tool.uv]
dev-dependencies = []
